MONGODB_URI=
MONGODB_DB_NAME=lumen-academy
ANTHROPIC_API_KEY=

# Optional. Overrides the AI Tutor model. Defaults to claude-sonnet-4-6.
TUTOR_MODEL=

# Auth.js v5 (Phase 4b). Generate AUTH_SECRET with: openssl rand -base64 32
AUTH_SECRET=
AUTH_URL=http://localhost:3000
EMAIL_FROM=no-reply@lumen.local
